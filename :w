.wrapper {
  border-right: 2px solid var(--background_main_inverted);
  width: 42px;
  overflow: hidden;
  transition: ease-in-out .2s;
}

.wrapper:hover {
  width: 18rem;
}

.nav {
  display: flex;
  flex-direction: column;
}

.name {
  padding-inline-start: 24px;
}

.sidebar_item_wrapper {
  display: flex;
  flex: column;
  align-items: center;
  border-bottom: 1px solid var(--background_main_inverted);
}

.sidebar_item_wrapper:hover {
  background-color: var(--background_secondary);
}

.icon {
  display: flex;
  justify-content: center;
  align-items: center;
  min-width: 42px;
  height: 42px;
}
